# Values for production-indo environment

# Use latest n8n version (1.121.3) which supports IPv6 Redis
image:
  tag: "1.121.3"

serviceAccount:
  create: false
  name: "n8n-gotrade-n8n"

n8n:
  protocol: "https"
  host: "n8n.admin.tr8.wiki"
  webhookUrl: "https://n8n.admin.tr8.wiki/"
  editorBaseUrl: "https://n8n.admin.tr8.wiki/"
  timezone: "Asia/Singapore"

# Ingress disabled - user will use other methods to access
ingress:
  enabled: false

# ALB Ingress disabled since ingress is disabled
albIngress:
  enabled: false

resources:
  limits:
    cpu: 1000m
    memory: 2Gi
  requests:
    cpu: 500m
    memory: 1Gi

persistence:
  enabled: true
  size: 20Gi

postgresql:
  enabled: true
  auth:
    username: n8n
    database: n8n
  primary:
    persistence:
      enabled: true
      size: 30Gi
    resources:
      limits:
        cpu: 500m
        memory: 1Gi
      requests:
        cpu: 250m
        memory: 512Mi

# Redis enabled with IPv6 support (n8n 1.121.3+ supports QUEUE_BULL_REDIS_DUALSTACK)
redis:
  enabled: true
  auth:
    enabled: true
  master:
    persistence:
      enabled: true
      size: 8Gi
    resources:
      limits:
        cpu: 250m
        memory: 512Mi
      requests:
        cpu: 100m
        memory: 256Mi

# Extra environment variables
extraEnv:
  # Enable IPv6 support for Redis connection
  - name: QUEUE_BULL_REDIS_DUALSTACK
    value: "true"

podLabels:
  tr8.io/landscape: production-indo

additionalLabels:
  tr8.io/landscape: production-indo

podAnnotations:
  tr8.io/landscape: production-indo

additionalAnnotations:
  tr8.io/landscape: production-indo

# Node selector for production-indo environment
nodeSelector:
  karpenter.sh/capacity-type: "spot"
  kubernetes.io/arch: "amd64"

# Tolerations for spot instances
tolerations:
  - key: "karpenter.sh/capacity-type"
    operator: "Equal"
    value: "spot"
    effect: "NoSchedule"
